name: zz-diag-callee-a
on:
  workflow_call:
    inputs:
      job:
        required: true
        type: string
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "job=${{ inputs.job }}"
